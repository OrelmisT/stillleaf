<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stilleaf</title>
    <link rel="icon" href="./still_leaf_logo.png">
    <link rel="stylesheet" href="./styles/password_reset_request.css">
</head>
<body>
    
    
    <% if (locals.email_sent) {%>
        
        <div id="container1">

            <div id="submission_confirmation">  
                <div style="display: flex; align-items: center; gap: 10px;">
                    <img id='logo' src="still_leaf_logo.png">
                    <h1 style="display: inline;">Stilleaf</h1>
                </div>
                <img id="checkmark" src="check_mark.png"></img>
                <h2>Check Your Email</h2>
                <p>We've sent a password reset link to your email. If you don't see the email, check your spam folder.</p>
                
                <button  id="return_to_login" type="submit">Return to Login</button>
            </div>
        </div>

    <%} else { %>

    <div id="container1">


        <form method="POST">
            <div style="display: flex; align-items: center; gap: 10px;">
                <img id='logo' src="still_leaf_logo.png">
                <h1 style="display: inline;">Stilleaf</h1>
            </div>
            <h2>Reset your password</h2>
            <p>Enter your email address below and we'll send you a link to reset your password.</p>
            <input type="email" placeholder="Email" value="<%= locals.email ? email : '' %>" required name="email">
            <button  id="reset" type="submit">Reset Password</button>
        </form>
    </div>




     <% if (locals.errorMessage) {%>
            <div class="error"> <%= errorMessage %>  <img id="close" style="position:absolute; right: 10px; width: 20px; top: 10px;" src="close.png"></img></div>
    <%}%>


    <% } %>

    <script src="./scripts/password_reset_request.js"></script>
    
</body>
</html>