<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stilleaf</title>
    <link rel="icon" href="./tree-green.png">
    <link rel="stylesheet" href="./styles/resetpassword.css">
</head>
<body>
    <nav>
        <div style="display: flex; align-items: center; gap: 15px;">
            <img id='tree' src="tree.png">
            <h1 style="display: inline;">Stilleaf</h1>
        </div>
    </nav>

   <% if (locals.errorMessage){ %>
        <h1><%=errorMessage%></h1>
   <% } else if (locals.success) { %>
       <h2>Request Password Reset</h2>
       <img id="leaf" src="leaf.jpg" width="400px">
       <p>Your password has been successfully reset</p>
    
    <% }else{ %>
        <div id="container1">
        <form method="POST">
            <h2>Reset Password</h2>
            <input type="password" placeholder="Enter New Password" required name="password">
            <input type="password" placeholder="Confirm New Password" required name="passwordconfirm">
            <button type="submit">Reset</button>
        </form>
    </div>
   <% } %>

   <script src="./scripts/resetpassword.js"></script>
    
</body>
</html>