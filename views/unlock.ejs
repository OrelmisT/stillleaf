<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StillLeaf</title>
    <link rel="stylesheet" href="./styles/unlock.css">
</head>
<body>
    <nav>
        <div style="display: flex; align-items: center; gap: 15px;">
            <img id='tree' src="tree.png">
            <h1 style="display: inline;">StillLeaf</h1>
        </div>
    </nav>
    <h2>Welcome Back!</h2>
    <img id="leaf" src="leaf.jpg" width="400px">
    <form method="POST">
        <input type="email" style="display: none;" name="email">
        <input type="password" placeholder="Enter Your Password" value="<%= locals.password ? password : '' %>" required name="password">
        <div style="display: flex; flex-direction: row; gap:20px; justify-content: center; margin-top: 5px;">
            <button id="unlock" type="submit">Unlock</button>
            <Button id="logout">Logout</Button>
            
        </div>
    </form>

    <% if (locals.errorMessage) {%>
            <div class="error"> <%= errorMessage %>  <img id="close" style="position:absolute; right: 10px; width: 20px; top: 10px;" src="close.png"></img></div>
    <%}%>

    <script src="./scripts/unlock.js"></script>
    
</body>
</html>